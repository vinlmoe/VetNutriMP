name: Build Debian Package

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
      - revetBackUp

jobs:
  build-deb:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for Gradle
        run: chmod +x gradlew

      - name: Build Debian package
        env:
          FOOD_DB_ENCRYPTION_KEY: ${{ secrets.FOOD_DB_ENCRYPTION_KEY }}
        run: ./gradlew --no-daemon :composeApp:packageReleaseDeb

      # Bloque la publication d'un .deb dont le runtime embarqué requiert
      # AVX2 (crash "invalid opcode" sur les CPU d'avant 2013).
      - name: Check embedded runtime targets generic x86-64
        run: bash scripts/check_deb_runtime.sh

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: VetNutriMP-deb
          path: composeApp/build/compose/binaries/main*/deb/*.deb
          if-no-files-found: error


